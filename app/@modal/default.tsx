/**
 * Nothing renders in the modal slot unless an event route is intercepted.
 * Next.js requires this file so the slot has a fallback on hard navigation.
 */
export default function ModalDefault() {
  return null;
}
